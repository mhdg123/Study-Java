실수형 자료 타입: float(4바이트), double(8바이트)
정수형 자료 타입의 기본형은 int
실수형 자료 타입의 기본형은 double

       double number1 = 3.141592;
       float number2 = 3.141592; //오류(기본값은 double이므로 - float 리터럴을 써줄려면
       float number2 =3.141592f; // float타입으로 뒤에 바꿔줘야함 //float타입이라는 걸 명시하기 위해 f를 붙여줘야함 

char: 문자 하나를 저장하기 위한 자료타입(2바이트)
char는 문자의 코드값(정수)가 담긴다.

        char ch1 ='A';
      문자열(String)은 큰따옴표("")로 묶어 줌
      문자 하나(char)는 작은따옴표('')로 묶어줌

System.out.println('z' - 'a' +1); //알파벳 갯수 //문자가 숫자로 변환되는 아스키코드가 있기 때문에 가능 //내부적으로 숫자로 변환해 연산

TextBLock: java 15 이상
-TextBLock 안에서는 굳이 "을 escaping 하지 않아도 된다

     String txt3= """
                <html>
                     <body>
                       <span>example text</span>
                     <body>
                <html>
                """;// """안에서는 이스케이프문자가 필요없음
